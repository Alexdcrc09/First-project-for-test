function convertNumbers(convert) {

  if (convert === 1) {
    return 'I';
  } if (convert === 'I') {
    return 1;
  }
  if (convert === 2) {
    return 'II';
  }
  if (convert === 'II') {
    return 2;
  }
  if (convert === 3) {
    return 'III';
  }
  if (convert === 'III') {
    return 3;
  }

}
const chiffres = [[1, 'I'], [2, 'II'], [3, 'III'], [4, 'IV'], [5, 'V'], [6, 'VI'], [7, 'VII'], [8, 'VIII'], [9, 'IX'], [10, 'X'], [20, 'XX'], [30, 'XXX'], [40, 'XL'], [50, 'L']]

chiffres.forEach((arabe, romain) => {
  exp()
  console.log(item) //value
  console.log(index) //index
})

module.exports = convertNumbers;